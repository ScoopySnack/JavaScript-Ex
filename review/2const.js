/**
 * JavaScript uses a dynamic policy, no datatypes needed. 
 * Variables are camelCase, and are declared using let, const, or var.
 * Js is case-sensitive.
 * Avoid declaring variables with var. Use let or const instead.
 * Take care of the syntax. Spaces between operators and operands are recommended.
 */



const PI = 3.14         //Declares 'true' constants
const maxThreads = 150  //Declares a 'contextual' variable
let counter = 0         //Declares non-constant variables
counter++

console.log(PI,typeof PI, maxThreads, counter)